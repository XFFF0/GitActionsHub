name: Build GitActionsHub IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_15.2.app
    
    - name: Show Xcode version
      run: xcodebuild -version
    
    - name: Build Archive (No Sign)
      run: |
        xcodebuild archive \
          -project GitActionsHub.xcodeproj \
          -scheme GitActionsHub \
          -configuration Release \
          -destination "generic/platform=iOS" \
          -archivePath build/GitActionsHub.xcarchive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          AD_HOC_CODE_SIGNING_ALLOWED=YES \
          COMPILER_INDEX_STORE_ENABLE=NO \
          | xcpretty
    
    - name: Export IPA (Unsigned)
      run: |
        mkdir -p build/ipa
        cp -r build/GitActionsHub.xcarchive/Products/Applications/GitActionsHub.app build/ipa/
        cd build/ipa
        mkdir Payload
        cp -r GitActionsHub.app Payload/
        zip -r GitActionsHub.ipa Payload
        rm -rf Payload GitActionsHub.app
    
    - name: Upload IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: GitActionsHub-IPA
        path: build/ipa/GitActionsHub.ipa
        retention-days: 30
